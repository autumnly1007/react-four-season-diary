import React from "react";
import styles from "./footer.module.scss";

const Footer = (props) => {
  return (
    <footer className={styles.footer}>
      <p className={styles.title}>made by autumn</p>
    </footer>
  );
};

export default Footer;
