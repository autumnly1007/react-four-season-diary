import React from "react";
import styles from "./nav.module.scss";

const Nav = (props) => {
  return (
    <section className={styles.nav}>
      <h1>Nav</h1>
    </section>
  );
};

export default Nav;
