import React from "react";
import styles from "./home.module.scss";

const Home = (props) => {
  return (
    <section className={styles.home}>
      <h1>Home</h1>
    </section>
  );
};

export default Home;
